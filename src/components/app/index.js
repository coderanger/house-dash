import React, { PropTypes } from 'react';
import Ticker from '../ticker';

const App = () => {
  const styles = require('./style.scss');
  return (
    <div>
      <Ticker/>
    </div>
  )
}

export default App
